import React from 'react'
import {
  ScrollView,
  SafeAreaView,
} from 'react-native'

const DetailsScreen = (props) => {
  return (
    <SafeAreaView>
      <ScrollView>
        Details Screen
      </ScrollView>
    </SafeAreaView>
  )
}


export default DetailsScreen
