import React from 'react'
import {
  ScrollView,
  StyleSheet,
  SafeAreaView,
} from 'react-native'

const DetailsScreen = (props) => {
  return (
    <SafeAreaView>
      <ScrollView>
        HOME SCREEN
      </ScrollView>
    </SafeAreaView>
  )
}


export default DetailsScreen
